name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  checks: write
  pull-requests: write
  issues: write

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  MAX_WASM_SIZE_KB: 100

# Cancel previous runs if a new one is triggered
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Job 1: Check code formatting
  format:
    name: Format Check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.85.0
          components: rustfmt

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-registry-

      - name: Check formatting
        run: cargo fmt --all -- --check

  # Job 2: Lint with Clippy
  lint:
    name: Lint (Clippy)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.85.0
          components: clippy
          targets: wasm32-unknown-unknown

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-clippy-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-clippy-

      - name: Run Clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Check Documentation
        run: cargo doc --no-deps
  # Job 3: Build the contract
  build:
    name: Build Contract
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: [format, lint]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.85.0
          targets: wasm32-unknown-unknown

      - name: Cache Stellar CLI
        uses: actions/cache@v4
        id: cache-stellar-cli
        with:
          path: ~/.cargo/bin/stellar
          key: ${{ runner.os }}-stellar-cli-${{ hashFiles('**/Cargo.lock') }}

      - name: Install system dependencies
        if: steps.cache-stellar-cli.outputs.cache-hit != 'true'
        run: sudo apt-get update && sudo apt-get install -y libdbus-1-dev pkg-config

      - name: Install Stellar CLI
        if: steps.cache-stellar-cli.outputs.cache-hit != 'true'
        run: cargo install --locked stellar-cli

      - name: Verify Stellar CLI
        run: stellar --version

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-build-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-build-

      - name: Build contract
        working-directory: fluxapay
        run: stellar contract build

      - name: Optimize WASM contract
        working-directory: fluxapay
        run: |
          for wasm in target/wasm32-unknown-unknown/release/*.wasm; do
            if [ -f "$wasm" ]; then
              stellar contract optimize --wasm "$wasm"
            fi
          done

      - name: List build artifacts
        run: ls -lh target/wasm32-unknown-unknown/release/*.wasm

      - name: Upload WASM artifact
        uses: actions/upload-artifact@v7
        with:
          name: fluxapay-contract-${{ github.sha }}
          path: target/wasm32-unknown-unknown/release/*.wasm
          retention-days: 30

      - name: Check WASM size
        run: |
          echo "### WASM Contract Size 📦" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Contract | Size |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|------|" >> $GITHUB_STEP_SUMMARY
          for wasm in target/wasm32-unknown-unknown/release/*.wasm; do
            if [ -f "$wasm" ]; then
              size=$(ls -lh "$wasm" | awk '{print $5}')
              name=$(basename "$wasm")
              echo "| $name | $size |" >> $GITHUB_STEP_SUMMARY
            fi
          done

      - name: Enforce WASM size limit
        run: |
          MAX_SIZE=$((MAX_WASM_SIZE_KB * 1024))
          echo "Max allowed size: $MAX_WASM_SIZE_KB KB ($MAX_SIZE bytes)"
          failed=0
          for wasm in target/wasm32-unknown-unknown/release/*.wasm; do
            if [ -f "$wasm" ]; then
              size=$(stat -c%s "$wasm")
              name=$(basename "$wasm")
              if [ "$size" -gt "$MAX_SIZE" ]; then
                echo "❌ Error: $name size is $size bytes, which exceeds the limit of $MAX_SIZE bytes!"
                failed=1
              else
                echo "✅ $name size is $size bytes (within limit)"
              fi
            fi
          done
          if [ "$failed" -eq 1 ]; then
            exit 1
          fi

  # Job 4: WASM Size Check
  wasm-size-check:
    name: WASM Size Check
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.85.0
          targets: wasm32-unknown-unknown

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-wasm-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-wasm-

      - name: Build contracts (release)
        working-directory: fluxapay
        run: |
          cargo build --target wasm32-unknown-unknown --release
          for feature in contract-payment-processor contract-refund-manager contract-gas-estimator contract-fx-oracle contract-merchant-registry contract-payment-link contract-payment-streaming; do
            cargo build --target wasm32-unknown-unknown --release --no-default-features --features "$feature"
          done

      - name: Check WASM sizes
        run: |
          MAX_SIZE=$((MAX_WASM_SIZE_KB * 1024))
          echo "Checking WASM contract sizes (max: $MAX_WASM_SIZE_KB KB = $MAX_SIZE bytes)"
          echo ""
          echo "### WASM Contract Sizes 📦" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Contract | Size (Bytes) | Size (KB) | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|--------------|-----------|--------|" >> $GITHUB_STEP_SUMMARY
          failed=0
          for wasm in target/wasm32-unknown-unknown/release/*.wasm; do
            if [ -f "$wasm" ]; then
              size=$(stat -c%s "$wasm")
              name=$(basename "$wasm")
              size_kb=$((size / 1024))
              if [ "$size" -gt "$MAX_SIZE" ]; then
                echo "❌ FAIL: $name is $size_kb KB ($size bytes) (exceeds limit by $((size_kb - MAX_WASM_SIZE_KB)) KB)"
                echo "| $name | $size | ${size_kb} KB | ❌ Exceeds limit ($((size_kb - MAX_WASM_SIZE_KB)) KB over) |" >> $GITHUB_STEP_SUMMARY
                failed=1
              else
                echo "✅ PASS: $name is $size_kb KB ($size bytes) (within limit)"
                echo "| $name | $size | ${size_kb} KB | ✅ Pass |" >> $GITHUB_STEP_SUMMARY
              fi
            fi
          done
          if [ "$failed" -eq 1 ]; then
            echo "Error: One or more WASM binaries exceeded the MAX_WASM_SIZE_KB limit ($MAX_WASM_SIZE_KB KB)"
            exit 1
          fi

  # Job 5: Run tests
  test:
    name: Test
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: [build]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.85.0
          targets: wasm32-unknown-unknown

      - name: Cache Stellar CLI
        uses: actions/cache@v4
        id: cache-stellar-cli
        with:
          path: ~/.cargo/bin/stellar
          key: ${{ runner.os }}-stellar-cli-${{ hashFiles('**/Cargo.lock') }}

      - name: Install system dependencies
        if: steps.cache-stellar-cli.outputs.cache-hit != 'true'
        run: sudo apt-get update && sudo apt-get install -y libdbus-1-dev pkg-config

      - name: Install Stellar CLI
        if: steps.cache-stellar-cli.outputs.cache-hit != 'true'
        run: cargo install --locked stellar-cli

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-test-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-test-

      - name: Run tests
        run: cargo test --verbose --all-features

  # Job 5b: Bounded property tests (separate job for independent failure reporting)
  proptest:
    name: Property Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: [build]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.85.0
          targets: wasm32-unknown-unknown

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-proptest-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-proptest-

      - name: Cache proptest regressions
        uses: actions/cache@v4
        with:
          path: fluxapay/proptest-regressions
          key: ${{ runner.os }}-proptest-regressions-${{ github.run_id }}
          restore-keys: |
            ${{ runner.os }}-proptest-regressions-

      - name: Run bounded property tests
        run: "PROPTEST_CASES=256 cargo test -p fluxapay proptests:: --all-features -- --test-threads=1"

      - name: Upload proptest regression cases
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: proptest-regressions-${{ github.sha }}
          path: fluxapay/proptest-regressions
          retention-days: 30

  # Final job: Report overall status
  ci-success:
    name: CI Success
    if: ${{ always() }}
    needs: [format, lint, build, wasm-size-check, test, proptest]
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check all jobs
        run: |
          if [[ "${{ needs.format.result }}" == "success" ]] && \
             [[ "${{ needs.lint.result }}" == "success" ]] && \
             [[ "${{ needs.build.result }}" == "success" ]] && \
             [[ "${{ needs.wasm-size-check.result }}" == "success" ]] && \
             [[ "${{ needs.test.result }}" == "success" ]] && \
             [[ "${{ needs.proptest.result }}" == "success" ]]; then
            echo "✅ All CI checks passed!"
            exit 0
          else
            echo "❌ Some CI checks failed"
            exit 1
          fi
